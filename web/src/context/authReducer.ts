import {
  LOGIN_REQUEST,
  LOGIN_SUCCESS,
  LOGIN_FAIL,
} from "../constants/AuthConstants";

export const authReducer = (state: any, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};
